# Data Notes

Use this directory for dataset-specific notes, not raw large files tracked in
git.

Suggested layout:

- `raw/`: downloaded source files, ignored by git
- `processed/`: cleaned intermediate files, ignored by git
- `dataset_cards/`: small text or markdown summaries if needed
